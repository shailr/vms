(function () {
  'use strict';

  angular
    .module('vms.todos', [
      'vms.todos.services',
      'vms.todos.controllers',
      'vms.todos.directives'
    ]);

  angular
    .module('vms.todos.services', []);

  angular
    .module('vms.todos.controllers', []);

  angular
    .module('vms.todos.directives', []);
})();
