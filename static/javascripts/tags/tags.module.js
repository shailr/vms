(function () {
  'use strict';

  angular
    .module('vms.tags', [
      'vms.tags.services',
      'vms.tags.controllers',
      'vms.tags.directives'
    ]);

  angular
    .module('vms.tags.services', []);

  angular
    .module('vms.tags.controllers', []);

  angular
    .module('vms.tags.directives', []);
})();
